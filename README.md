# temp-ipsy
Temporary ipsy-docs website

## Build pages locally

Build setup:
```bash
python -m venv venv
. ./venv/bin/activate
pip install mkdocs
```

Build page
```bash
mkdocs serve
```

Open up `http://127.0.0.1:8000/` in your browser
